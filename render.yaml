services:
  - type: web
    name: leadforge-maps-scraper
    env: docker
    plan: free # Note: 512MB RAM — tight for headless Chromium, keep max_results low    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 8000
